#include<iostream>
int sort(int arr[],int n);
using namespace std;
int main()
{
  int n,k;
  cin>>n>>k;
  while(n>0)
  {
  aux[i]=n/10;
  n=n/10;
  i++;
  }
  sort(aux);
  for(int i=0;i<=(n-k);i++)
  cout<<aux[i];
}
int sort(int arr[],int n);
{
  int key,i,j;
  for(int i=0;i<n;i++)
  {
    key=a[i];
    j=i-1;
    while(j>=0 && a[j]>key)
    {
      a[j+1]=a[j];
      j=j-1;
      
    }
    arr[j+1]=key;
    
  }
  

}
  
